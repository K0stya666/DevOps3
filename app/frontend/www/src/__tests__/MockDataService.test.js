import MockDataService from '../MockDataService';

// ћинимальный тест, чтобы избежать ошибок
describe('MockDataService', () => {
  it('should exist', () => {
    expect(MockDataService).toBeDefined();
  });
});